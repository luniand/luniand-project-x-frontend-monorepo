{
  "private": true,
  "name": "students",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev --https --ssl-key key.pem --ssl-cert cert.pem --port 3010",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "firebase": "^9.18.0",
    "firebase-admin": "^11.5.0",
    "nuxt": "^3.3.2"
  }
}
